import { describe, expect, it } from 'vitest'

describe('client', () => {
  it('should pass', () => {
    expect(2 + 2).toBe(4)
  })

  it('should also pass', () => {
    expect(3 + 3).toBe(6)
  })
})
